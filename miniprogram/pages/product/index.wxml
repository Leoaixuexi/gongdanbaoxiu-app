<view class="product-page">
  <product-list
    id="productList"
    canManage="{{canManage}}"
    bind:itemtap="onProductTap"
  />

  <view wx:if="{{canManage}}" class="fab-button" bindtap="onAddProductTap">
    <van-icon name="plus" size="24px" color="#FFFFFF" />
  </view>
</view>
