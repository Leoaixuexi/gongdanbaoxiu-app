<!--修改工单页面 - 提交页面风格-->
<view class="submit-container">
  <!-- 自定义导航栏 -->
  <custom-header title="编辑工单" />

  <!-- 表单卡片列表 -->
  <view class="form-cards" style="padding-top: calc({{headerHeight}}px + 24rpx);">
    <!-- 工单编号 -->
    <view class="card">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">工单编号</text>
      </view>
      <view class="card-right">
        <input
          class="card-input order-number-input has-value"
          value="{{formData.orderCode}}"
          disabled="{{true}}"
        />
      </view>
    </view>

    <!-- 楼层 -->
    <view class="card">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">楼层</text>
      </view>
      <picker mode="selector" range="{{floorOptions}}" bindchange="handleFloorChange">
        <view class="card-picker">
          <text class="picker-text {{formData.floor ? '' : 'placeholder'}}">{{formData.floor || '请选择'}}</text>
        </view>
      </picker>
    </view>

    <!-- 具体位置 -->
    <view class="card">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">具体位置</text>
      </view>
      <input
        class="card-input"
        placeholder="请填写"
        placeholder-class="custom-placeholder"
        value="{{formData.location}}"
        bindinput="handleLocationChange"
      />
    </view>

    <!-- 工单类别 -->
    <view class="card" bindtap="handleCategorySelect">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">工单类别</text>
      </view>
      <view class="card-picker">
        <text class="picker-text {{formData.category ? '' : 'placeholder'}}">{{formData.category || '请选择'}}</text>
      </view>
    </view>

    <!-- 责任方 -->
    <view class="card" bindtap="handleResponsibleSelect">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">责任方</text>
      </view>
      <view class="card-picker">
        <text class="picker-text {{formData.responsible ? '' : 'placeholder'}}">{{formData.responsible || '请选择'}}</text>
      </view>
    </view>

    <!-- 优先级 -->
    <view class="card">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">优先级</text>
      </view>
      <view class="priority-row">
        <view
          wx:for="{{priorityOptions}}"
          wx:key="key"
          class="priority-btn {{selectedPriority === item.key ? 'active-' + item.color : ''}}"
          bindtap="onPrioritySelect"
          data-key="{{item.key}}"
        >
          <text class="priority-text">{{item.label}}</text>
        </view>
      </view>
    </view>

    <!-- 日期时间 -->
    <view class="card">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">报修时间</text>
      </view>
      <view class="datetime-display" bindtap="showDateTimePicker">
        <text class="datetime-text">{{formData.reportTime}}</text>
      </view>
    </view>

    <!-- 工单状态 (只读显示) -->
    <view class="card">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">工单状态</text>
      </view>
      <view class="card-right">
        <text class="card-display-text">{{formData.status}}</text>
      </view>
    </view>

    <!-- 报修人 (只读显示) -->
    <view class="card">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">报修人</text>
      </view>
      <view class="card-right">
        <text class="card-display-text">{{formData.reporter}}</text>
      </view>
    </view>

    <!-- 问题描述 -->
    <view class="card card-textarea">
      <view class="card-label-required">
        <text class="star">*</text>
        <text class="label">问题描述</text>
      </view>
      <textarea
        class="textarea-box"
        placeholder="请详细描述遇到的问题..."
        placeholder-class="custom-placeholder"
        value="{{formData.description}}"
        bindinput="handleDescriptionChange"
        maxlength="50"
      />
    </view>

    <!-- 现场照片 -->
    <view class="card card-photos">
      <view class="photos-header">
        <view class="card-label-required">
          <text class="star">*</text>
          <text class="label">现场照片</text>
        </view>
        <text class="photos-tip">最多3张</text>
      </view>
      <view class="photos-row">
        <view wx:for="{{photoSlots}}" wx:key="index" class="photo-box">
          <view wx:if="{{item}}" class="photo-preview">
            <image class="photo-img" src="{{item}}" mode="aspectFill" bindtap="handlePreviewImage" data-index="{{index}}" />
            <view class="photo-del" bindtap="handleRemoveImage" data-index="{{index}}">
              <text class="del-text">×</text>
            </view>
          </view>
          <view wx:else class="photo-add" bindtap="handleAddImage" data-index="{{index}}">
            <text class="add-plus">+</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 备注 -->
    <view class="card card-textarea">
      <view class="card-label-wrapper">
        <text class="label">备注</text>
      </view>
      <textarea
        class="textarea-box textarea-box-remark"
        placeholder="请填写备注信息（非必填）"
        placeholder-class="custom-placeholder"
        value="{{formData.remarks}}"
        bindinput="handleRemarksChange"
        maxlength="30"
      />
    </view>
  </view>

  <!-- 底部按钮 -->
  <view class="bottom-btns">
    <view class="btn-cancel" bindtap="handleCancel">
      <text class="btn-cancel-text">取消</text>
    </view>
    <view class="btn-submit" bindtap="handleSave">
      <text class="btn-submit-text">保存修改</text>
    </view>
  </view>

  <!-- 日期时间选择弹窗 -->
  <view wx:if="{{isDateTimePickerOpen}}" class="modal-overlay" catchtap="closeDateTimePicker">
    <view class="modal-content" catchtap="stopPropagation">
      <view class="modal-header">
        <text class="modal-title">选择日期时间</text>
      </view>

      <view class="datetime-picker-content">
        <view class="picker-row">
          <text class="picker-label">日期</text>
          <picker mode="date" value="{{tempDate}}" bindchange="onTempDateChange">
            <view class="datetime-picker-display">
              <text class="datetime-picker-text">{{tempDate || '请选择日期'}}</text>
            </view>
          </picker>
        </view>
        <view class="picker-row">
          <text class="picker-label">时间</text>
          <picker mode="time" value="{{tempTime}}" bindchange="onTempTimeChange">
            <view class="datetime-picker-display">
              <text class="datetime-picker-text">{{tempTime || '请选择时间'}}</text>
            </view>
          </picker>
        </view>
      </view>

      <view class="modal-actions">
        <view class="modal-button modal-button-cancel" bindtap="cancelDateTimePicker">
          <text class="modal-button-text">取消</text>
        </view>
        <view class="modal-button modal-button-confirm" bindtap="confirmDateTimePicker">
          <text class="modal-button-text modal-button-text-white">确定</text>
        </view>
      </view>
    </view>
  </view>
</view>
